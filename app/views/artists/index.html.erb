<div id="pagediv">

	<div id="titlediv">
    <h1>ARTISTS</h1>
  </div>
	
	<div id="gallerycontent">
	  <% @users.each do |user| %>
	      <%= link_to (image_tag user.avatar.url(:thumb)), user %>
	      <h3><%= link_to user.name, user %></h3>
	  <% end %>
  </div>
</div>